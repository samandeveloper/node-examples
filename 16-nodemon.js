//we want to install the nodemon npm packagh to prevent every time typing node... to execute the project
//npm i nodemon  =>this will add nodemon to the dependencies in package.json
//npm i nodemon -D //or npm i nodemon --save-dev =>this will add the package to the devDependencies in the package.json

//we can install the nodemon in devDependencies since we don't need it in the production


